# Copy this file to .proxy.env and fill in your proxy values when needed.
# The setup scripts load it automatically if present.

HTTP_PROXY=http://proxy.example.com:8080
HTTPS_PROXY=http://proxy.example.com:8080
NO_PROXY=localhost,127.0.0.1,::1
